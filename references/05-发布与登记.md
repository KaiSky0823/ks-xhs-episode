# 05 · 发布与登记

## 发布前检查

1. **主动勾选 AI 生成标识** —— 底图是 Seedream 生成的。小红书 2026-07-14 起强制检测，主动标对推荐更友好。
2. **四张的顺序** —— 钩子 / 答案 / 机制 / 彩蛋，别传错。
3. **封面裁切** —— 首图如果带操作说明或关键标注，上传后预览一眼别被裁掉。
4. **健康类选题的安全提醒不能删**。
5. 标题含敏感词（交配之类）审核可能卡，准备好备选。

## 发布后登记三处 ← 当场做，别攒

| 处 | 改什么 |
|---|---|
| `$LIB` 选题库 | 那一集的标题改成「第 N 集 已发布（日期）」 |
| `第N集-XX/发布文案.md` | 顶部加一行「已发布（日期）」；**如果 Kai 改了标题/正文，加一节「实际发布」记下来** |
| `xhs-macmini-mbp/同步台账.md` | 如果这集动了 kit 里的文件（色板、门禁、模板），当场加一行待同步 |

**实际发布版要记**。第 3 集 Kai 把标题换成问句、正文整个不要，这条观察后来变成了文案 SOP。
不记下来，下次还会给错方向的稿。

## 色板登记

每集换色板都要在 `$KIT/KAI_VISUAL_DNA.md` 的色板表加一行：
名称 / PAPER 标称值 / **CIE L\*** / INK / 强调 / 色块 / 用于第几集（附底图实测均色）。

**纸色取底图实测均色，不是标称值**。差一点点，SVG 主体的纹理线就会浮出来。

```python
# 实测均色（避开右上网点区）
region = np.concatenate([im[:, :w//2].reshape(-1,3), im[h//2:, :].reshape(-1,3)])
```

## mac mini 同步

改了 `$KIT` 里任何文件 → 当场在同步台账「待同步」加一行。
出全量包的命令和差异自检脚本在台账里。**只出全量包，不做增量。**

`persona.md` / `SOUL.md` / `garyvee-mindset.md` 三个文件**事实源在 mini**，MBP 只存镜像，全量包不覆盖它们。
